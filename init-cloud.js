#!/usr/bin/env node

// 直接调用云函数初始化数据库
const https = require('https');

// 这个脚本需要在微信开发者工具运行时执行
console.log('正在调用云函数 init 初始化数据库...');

// 使用 HTTP API 调用云函数
// 注意：这只是一个提示脚本，实际需要通过开发者工具界面操作

console.log(`
===========================================
请在微信开发者工具中执行以下操作：

1. 点击「云开发」按钮打开云开发控制台
2. 点击左侧「云函数」菜单
3. 找到 ` + '`init`' + ` 云函数
4. 右键点击选择「在云端安装依赖」（如果需要）
5. 点击「运行」按钮运行云函数

或者直接在小程序中：
1. 清除缓存（工具 -> 清除缓存 -> 清除全部数据）
2. 重新编译小程序
3. 小程序会自动调用 init 云函数初始化数据库
===========================================
`);

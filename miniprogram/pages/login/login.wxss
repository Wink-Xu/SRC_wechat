/* pages/login/login.wxss */

page {
  --green: #C8FF54;
}

.container {
  min-height: 100vh;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  padding-bottom: 60rpx;
  background: #000000;
}

/* 背景图片 - 自适应 */
.bg-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

/* 按钮区域 */
.btn-section {
  position: relative;
  z-index: 1;
  width: 100%;
  padding: 0 40rpx;
}

/* 登录按钮 */
.login-btn {
  width: 100%;
  height: 96rpx;
  background: var(--green);
  color: #1A1A1A;
  font-size: 32rpx;
  font-weight: 700;
  border: none;
  border-radius: 48rpx;
  box-shadow: 0 8rpx 24rpx rgba(200, 255, 84, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.login-btn::after {
  border: none;
}

.login-btn:active {
  transform: scale(0.98);
}